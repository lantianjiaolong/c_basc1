#include <cs50.h>
#include <stdio.h>

/**
 * @brief 主函数，无限循环输出 "meow" 字符串。
 *
 * @param void 无参数。
 * @return int 程序正常退出时返回 0（实际上该函数不会正常返回）。
 */
int main(void)
{

    /* 无限循环，持续打印 "meow" 并换行 */
    while (true)
    {
        printf("meow\n");
    }
}